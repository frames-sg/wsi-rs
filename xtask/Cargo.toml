[package]
name = "xtask"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
wsi-rs-perf = { path = "../perf-runner" }
wsi-rs-test-support = { path = "../test-support" }

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
